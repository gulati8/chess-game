services:
  chess-game:
    image: ghcr.io/gulati8/chess-game:latest
    container_name: chess-game
    restart: unless-stopped
    env_file: .env
    networks:
      - proxy

networks:
  proxy:
    external: true
